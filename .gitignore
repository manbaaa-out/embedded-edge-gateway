# ---- 构建产物 ----
build/
build*/
*.o
*.a
*.so

# CMake 用户本地覆盖(preset 的个人化配置,不入库)
CMakeUserPresets.json

# ---- 运行期产物 ----
*.db
*.db-shm
*.db-wal
*.log

# ---- 编辑器 / 工具 ----
.vscode/
.idea/
.cache/
compile_commands.json

# WSL mark-of-the-web 垃圾(NTFS ADS)
*:Zone.Identifier

# CMake 配置期从 web/ 资源生成的嵌入头(产物,不入库;源在 web/ + WebAsset.h.in)
**/WebAsset.h
